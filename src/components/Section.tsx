// Компонент Section — це універсальна обгортка для секцій, яка додає ефект плавної появи при скролі. Він робить сайт більш живим і сучасним без зайвого коду: достатньо просто обгорнути будь-який блок у <Section>.



// Імпорт motion з бібліотеки framer-motion для анімацій
import { motion } from "framer-motion";

// Імпорт React для створення компонентів
import React from "react";

// Оголошення інтерфейсу пропсів для компонента Section
// children — будь-які дочірні елементи, які будуть відображені всередині Section
// className — необов’язковий пропс для додавання власних CSS-класів
interface SectionProps {
  children: React.ReactNode;
  className?: string; // 👈 додали className, щоб можна було передавати стилі
}

// Функціональний компонент Section
const Section: React.FC<SectionProps> = ({ children, className }) => {
  return (
    // motion.div — це div з анімацією від framer-motion
    <motion.div
      className={className} // 👈 тепер можна передавати стилі ззовні

      // Початковий стан анімації: прозорість 0, зміщення вниз на 50px
      initial={{ opacity: 0, y: 50 }}

      // Стан при появі у viewport: прозорість 1, повернення на місце (y: 0)
      whileInView={{ opacity: 1, y: 0 }}

      // Налаштування швидкості анімації: тривалість 0.8 секунди
      transition={{ duration: 0.8 }}

      // viewport — налаштування, коли анімація запускається:
      // once: true → анімація виконується лише один раз
      // amount: 0.1 → елемент має бути видимим хоча б на 20%, щоб анімація запустилась
      viewport={{ once: true }}
    >
      {/* Вміст секції (діти, які передаються у компонент) */}
      {children}
    </motion.div>
  );
};

// Експорт компонента, щоб його можна було використати в інших файлах
export default Section;
